{
  "buildCommand": "cd apps/web && pnpm install && pnpm exec prisma generate && DISABLE_ESLINT_PLUGIN=true next build --output-directory out",
  "outputDirectory": "apps/web/out",
  "installCommand": "pnpm install"
}